node_modules/

# Credenciais. Nunca versionar.
.env
.env.local
.env*.local
!.env.local.exemplo

.next/
out/
build/
.vercel/

# Build info do TypeScript (derivado).
tsconfig.tsbuildinfo

# Config local do Claude Code (permissoes por maquina).
.claude/

# Export do workflow n8n. Fica local — contem PII no pinData (dado de teste) e
# nao e codigo da aplicacao. Gerenciado direto no n8n.
agente ia workflow.txt

# Assets-fonte da marca. As copias que o app usa vivem em public/. A pasta fonte
# fica local (nao precisa no repo publico).
[Rr]ebranding/

*.log
npm-debug.log*

.DS_Store
Thumbs.db
